import 'package:flutter/material.dart';

/// Página de lista de salidas grupales
class SalidaListPage extends StatelessWidget {
  const SalidaListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Salidas Grupales'),
      ),
      body: const Center(
        child: Text('Salida List Page - Por implementar'),
      ),
    );
  }
}

