import 'package:flutter/material.dart';

/// Página de creación de ruta (multi-step)
class CreateRutaPage extends StatelessWidget {
  const CreateRutaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Crear Ruta'),
      ),
      body: const Center(
        child: Text('Create Ruta Page - Por implementar'),
      ),
    );
  }
}

