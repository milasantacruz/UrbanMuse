import 'package:flutter/material.dart';

/// Página del Top 10 del usuario
class Top10Page extends StatelessWidget {
  const Top10Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mi Top 10'),
      ),
      body: const Center(
        child: Text('Top 10 Page - Por implementar'),
      ),
    );
  }
}

